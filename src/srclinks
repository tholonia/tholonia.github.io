#!/bin/bash

ln -fs ${THOLONIA_SITE_ROOT}/src/SERVE ${THOLONIA_SITE_ROOT}
ln -fs ${THOLONIA_SITE_ROOT}/src/FINDFILE ${THOLONIA_SITE_ROOT}
ln -fs ${THOLONIA_SITE_ROOT}/src/FINEINFILE ${THOLONIA_SITE_ROOT}

